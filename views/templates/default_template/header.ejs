<!DOCTYPE html>
<html>
  <head>
    <title><%= locals.title %></title>
    <link rel="icon" 
      href="img/icons/<%= (locals.favicon) ? favicon.path_name : "favicon" %>.<%= (locals.favicon) ? favicon.ext : "png" %>" 
        type="image/<%= (locals.favicon) ? favicon.ext : "png" %>" 
          sizes="16x16"> 
    <%- include(__global.linkCSS) %> 
  </head>
  <body>
    <div class="container">